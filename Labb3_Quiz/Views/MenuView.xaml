<UserControl x:Class="Labb3_Quiz.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:local="clr-namespace:Labb3_Quiz.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Menu>
        <MenuItem Header="_File">
            <MenuItem Header="_New Question Pack" Icon="{fa:Icon FolderPlus}"/>
            <MenuItem Header="_Select Question Pack" Icon="{fa:Icon FolderOpen}"/>
            <MenuItem Header="_Delete Question Pack" Icon="{fa:Icon FolderMinus}"/>
            <MenuItem Header="_Import Questions" Icon="{fa:Icon CloudArrowDown}" InputGestureText="Ctrl+I" />
            <Separator />
            <MenuItem Header="E_xit" Icon="{fa:Icon DoorOpen}" InputGestureText="Alt+F4" />
        </MenuItem>
        <MenuItem Header="_Edit">
            <MenuItem Header="Add Question" Icon="{fa:Icon Plus}" InputGestureText="Insert" />
            <MenuItem Header="Remove Question" Icon="{fa:Icon Minus}" InputGestureText="Delete"/>
            <MenuItem Header="Set PackName" Command="{Binding PlayerViewModel.SetPackNameCommand}" InputGestureText="Ctrl+O" Icon="{fa:Icon Sliders}" />
        </MenuItem>
        <MenuItem Header="View">
            <MenuItem Header="_Play" InputGestureText="Ctrl+P" Icon="{fa:Icon Play}" />
            <MenuItem Header="_Edit" InputGestureText="Ctrl+E" Icon="{fa:Icon Gear}" />
            <MenuItem Header="_Fullscreen" InputGestureText="Alt+Enter" Icon="{fa:Icon Expand}" />
        </MenuItem>
        
    </Menu>
</UserControl>
